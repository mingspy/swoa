@{
    Layout = null;
    string json = Request.Form["json"];
    string msg = new YJ.Platform.WorkFlow().InstallFlow(json);
    YJ.Platform.Log.Add("安装了流程", json + "(" + msg + ")", YJ.Platform.Log.Types.流程相关);
    Response.Write(msg);
}
